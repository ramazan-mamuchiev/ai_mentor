[pytest]
testpaths =
    doc2md-mcp/tests
addopts = --import-mode=importlib
asyncio_mode = auto
markers =
    e2e: end-to-end integration tests (real conversions)
    slow: slow tests (e.g. OCR model loading)
